{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Edge against localhost",
            "url": "http://localhost:8892/CREATE_FORM/form_create_user.html",       
            "webRoot": "${workspaceFolder}"
        }
    ]
}