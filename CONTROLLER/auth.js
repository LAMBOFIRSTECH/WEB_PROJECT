const config = require('../config');

// Utiliser la valeur apiUrl
console.log('La valeur de apiUrl est :', config.apiUrl);


// var url_api=config.apiUrl();
// config.apiUrl.require()

// async function useAPi(url){
//     console.log(url);

// }
// useAPi(url_api);
